schemaVersion: 1.1.0
name: wiring-app
version: 0.1.0
registry: "localhost:5000"

mixins:
  - exec

parameters:
  - name: connstr
    type: string
    default: ""

install:
  - exec:
      command: echo
      arguments:
        - "installed wiring-app"

uninstall:
  - exec:
      command: echo
      arguments:
        - "uninstalled wiring-app"
