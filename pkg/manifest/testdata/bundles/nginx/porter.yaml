mixins:
- exec

name: nginx
version: 1.19
tag: localhost:5000/nginx

install:
  - exec:
      description: "install nginx"
      command: bash
      flags:
        c: '"echo Hello nginx"'

uninstall:
  - exec:
      description: "uninstall nginx"
      command: bash
      flags:
        c: '"echo Hello nginx"'

custom:
  foo: bar
