schemaVersion: 1.0.0
version: 0.1.0
registry: example.com
name: mybun

mixins:
  - exec

install:
  - exec: install something

uninstall:
  - exec:
      description: uninstall everything
      command: bash