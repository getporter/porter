schemaVersion: 1.0.0
version: 0.1.0
registry: example.com

install:
  - exec:
      description: Install something
      command: bash

uninstall:
  - exec:
      description: uninstall everything
      command: bash