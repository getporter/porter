actions:
  install:
    - exec:
        description: Run a script
        command: ./install.sh
    - exec:
        description: Install Hello World
        command: bash
        flags:
          c: echo Hello World
